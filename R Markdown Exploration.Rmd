---
title: "R Markdown Exploration"
output: html_document
---

This is a code block - __r__ is naming programming language. The lower code is then a section of R code. 

```{r} 
install.packages("rmarkdown")
install.packages

install.packages('gapminder')
install.packages('DT')
install.packages('tibble')


library(gapminder)
library(DT)
library(tibble)
```




```{r}
mtcars

```{r}


